[defaults]
inventory       = inventory/hosts.ini
# Playbooks live in playbooks/; point them at the sibling roles/ directory.
roles_path      = roles
host_key_checking = False
retry_files_enabled = False
interpreter_python = auto_silent

[ssh_connection]
pipelining = True
