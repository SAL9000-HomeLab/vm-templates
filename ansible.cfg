# Lets tools run from the repository root (ansible-lint in CI, editors) resolve
# the roles used by ansible/playbooks/. Running ansible-playbook from ansible/
# uses ansible/ansible.cfg instead.
[defaults]
roles_path = ansible/roles
